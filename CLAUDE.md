# CLAUDE.md — Navigation map for Sungrid Protocol

This repo follows the [OpenRAModSDK](https://github.com/OpenRA/OpenRAModSDK) pattern: the OpenRA engine is a pinned, fetched build dependency (`engine/`, gitignored, not committed), not vendored source. Read this first before exploring the tree.

## What this repo is

`richardkfm/sungrid-protocol` is a Mod SDK-style repo for Sungrid Protocol, a solarpunk reinterpretation of the classic Red Alert RTS formula, built on the OpenRA engine. It started as a direct fork of the full OpenRA engine repository, but that was superseded in Phase 0 (before any mod content existed) in favor of the standard SDK pattern — see `docs/ARCHITECTURE.md` for the full rationale.

## Directory map

**Fetched engine dependency — never commit, never edit directly:**
- `engine/` — downloaded/built by `fetch-engine.sh` (or `make`), pinned via `mod.config`'s `ENGINE_VERSION`. Contains `OpenRA.Game`, `OpenRA.Mods.Common`, stock mods (`mods/ra`, `mods/cnc`, `mods/d2k`, `mods/ts`), etc. Gitignored — if a friction point genuinely needs an engine-level change, it usually does **not** need a separate personal fork repo: this repo's own pre-Phase-0 history already contains the full engine tree, so a fix can be pinned via an `engine-patch/*` branch built on the currently-pinned commit — see "Engine version pinning" below before touching `ENGINE_VERSION` or any `engine-patch/*` branch. Only reach for an actual personal fork of `OpenRA/OpenRA` if the needed base commit genuinely isn't already reachable in this repo's own history (see `docs/ARCHITECTURE.md`).

**Mod/content territory — where Sungrid Protocol work actually happens:**
- `mods/sungrid/` — **the Sungrid Protocol mod content** (rules/YAML, sequences, maps, chrome, fluent strings). Currently the SDK's example-mod template renamed to Sungrid branding — Phase 1's job is to replace this placeholder content with real gameplay forked from `mods/ra` (pulled from `engine/mods/ra` once fetched, or the public OpenRA/OpenRA repo).
- `OpenRA.Mods.Sungrid/` — mod-specific C# project. Currently the SDK's example stub traits (`ColorPickerColorShift`, `PlayerColorShift`), renamed. This is where the Phase 3 Grid Reserve trait will live.
- `mod.config`, `fetch-engine.sh`/`.cmd`, `Makefile`/`make.cmd`/`make.ps1`, `launch-game.*`, `launch-dedicated.*`, `utility.*`, `Sungrid.sln`, `packaging/` — SDK scaffolding, all mod-scale (not the engine's own build/packaging tooling).

**Design docs:**
- `docs/BLUEPRINT.md` — master document, full project blueprint in one place.
- `docs/VISION.md` — design pillars, tone, what differentiates Sungrid Protocol from vanilla RA.
- `docs/ROADMAP.md` — phase-by-phase plan (0 through 6+) with deliverables, exit criteria, explicit non-goals.
- `docs/ARCHITECTURE.md` — OpenRA technical strategy, data-driven vs. engine-level split, known friction points.
- `docs/GAME_MODES.md` — full spec for the Grid Reserve economic victory mode.
- `docs/BUILDINGS.md` — the 10-building initial roster, categorized and staged.
- `docs/ART_DIRECTION.md` — solarpunk tone/visual guardrails. `docs/concept-art/` holds non-canonical HTML concept sketches (currently the Phase 5 building dossier and its faux-pixel-art follow-up) — discussion drafts, not shippable assets — plus two PNG review renders of the actual shipped `gen_concept_art.py` output (a contact sheet and a terrain-readability check) referenced inline from `docs/ART_DIRECTION.md`'s issue #40 write-up.
- `docs/CONTRIBUTING.md` — Sungrid-specific workflow (branches, labels, RFCs, PR checklist). Root `CONTRIBUTING.md` is the Mod SDK's own contributing guidelines (still points to OpenRA's coding-standard wiki for engine-level style).
- `docs/LICENSE_NOTES.md` — GPLv3 inheritance, EA non-affiliation, original-asset licensing notes.

## Project goals and principles (condensed from `docs/VISION.md`)

1. Classic RTS legibility first — theme never overrides silhouette/UI readability.
2. Solarpunk as a lens that changes gameplay (power management, recycling, grid defense), not just reskinned names.
3. Spend vs. save is a first-class strategic decision (this is what Grid Reserve is for).
4. Pressure never disappears — scouting, harassment, raiding, and map control stay relevant in every mode, including the economic one.
5. Data-driven before engine-driven — YAML/Lua first, new C# only when provably necessary.
6. Small, reversible phases — every phase ships something playable and can be rolled back cleanly.
7. Destruction victory stays intact — Grid Reserve is additive and optional, never a replacement.
8. 3+ players is the target for new social/economic mechanics — diplomacy and shared-resource systems are deliberately deferred until this is validated.

## Key architecture decisions already made

- **Mod SDK pattern, not a vendored engine fork** — the engine is fetched/pinned via `mod.config` + `fetch-engine.sh`, not committed to this repo. Superseded the original "full engine fork" decision before any mod content existed (see `docs/ARCHITECTURE.md` for the full reasoning).
- Data-driven-first: buildings/units/rules go through YAML composing existing OpenRA traits wherever possible; new C# traits (in `OpenRA.Mods.Sungrid/`) are reserved for things YAML genuinely can't express (currently: only the Grid Reserve vault/win-condition mechanic).
- Destruction victory is the permanent default; Grid Reserve is a toggleable lobby option.
- "Grid Reserve" is the recommended/working name for the economic victory mode (see `docs/GAME_MODES.md` for the other 4 candidates that were considered).
- Diplomacy, alliance mechanics, and shared/pooled resources are explicitly out of scope until Phase 3-5 playtests justify them (see Phase 8+ in `docs/ROADMAP.md`).

## Current status

Phases 0-3 and 5 are functionally complete and confirmed playable: `mods/sungrid` holds real Red Alert-derived gameplay (not the SDK's placeholder example content), the full Phase 2 econ buildings and Phase 5 building roster (Cryptominer, Datacenter for AI, Drone Bay, Grid Defense Turret, Smart Grid Relay, Resilience Shelter, Sensor Array), and the Grid Reserve economic victory mode (deposit/hold-to-win, HUD bars, Lockdown countdown, minimap reveal) are all implemented and merged to `main`. A first real local build/launch/skirmish playtest (see `docs/PLAYTESTING.md`) found and fixed 4 previously-undetected bugs invisible to CI (an invalid `FluentReference` attribute, an actor-id collision breaking rules loading, a nonexistent `ServerTraits` class, a missing sprite icon sequence) — see PR #16. Remaining Phase 5 work is real custom art (still stock/reused RA sprites throughout — see `docs/BACKLOG.md` issue #12) and the broader visual/audio identity overhaul now scoped as Phases 6/7 in `docs/ROADMAP.md`. Since that PR, three more content/tone passes merged: Flame Infantry/Tower replaced with Disruptor Trooper/Arc Turret (issue #14), the two umbrella sides renamed Allies→The Consortium and Soviet→The Assembly (issue #15), and a first-boot polish pass fixing a shellmap regression and replacing the stock Soviet icon (issue #16). Phase 6's chrome is now fully original art (`docs/BACKLOG.md` issue #41, superseding issue #13's stock-art recolor): `mods/sungrid/uibits/gen_chrome.py` generates `dialog.png`/`sidebar.png`/`loadscreen*.png` and the mod icons from scratch in the locked palette — zero stock-derived pixels, a redesigned sun-over-horizon-in-hex emblem, Consortium-gold/Assembly-green faction accents, all `chrome.yaml` rects unchanged (see `mods/sungrid/uibits/PLACEHOLDER_ART.md`; still programmatic, not a human-designer pass). Cursors and the main-menu shellmap are still stock content, but the `.shp`-format encode/decode blocker itself is now resolved and verified (issue #17) — a real engine build + a full `.shp` cursor and `.tem` terrain tile round-trip both succeeded in a from-scratch environment, so what's left there is an art/wiring pass, not an access problem. All three terrain tilesets now have a first-pass palette-only reskin done (issue #18): `mods/sungrid/rules/palettes.yaml`'s terrain palettes point at new mod-owned `.pal` files recoloring the dominant tan/dirt/rust band toward the locked green while preserving gold ore-glint accents, water, and rock/road grays. Temperate and snow use the recolor at full strength; desert needed a hard brightness cutoff (`--max-value=0.5`) since its dominant sand color itself sits in the touched hue band and a full-strength or gradually-tapered shift both erased the desert identity into solid green — the cutoff keeps bright dune sand sandy and only shifts shadowed crevices. All three verified via composited sprite-sheet renders; a live-client camera view has been attempted twice (including a longer stabilization wait) and still isn't possible without the `Launch.SkirmishBots` engine patch from `docs/PLAYTESTING.md` — the no-bots quick-load path has no starting units anywhere on the map, so there's no vision at all, not a rendering bug. Both open decisions from the issue #17 verification are now resolved: `ENGINE_VERSION` is re-pinned (issue #20) to a commit fixing the `CryptoUtil.SHA1Hash` ambiguity (and a follow-up `IDE0301` conflict the fix itself triggered under CI's SDK), verified via a real CI run on both Linux and Windows; and GitHub Actions CI is confirmed not disabled — a docs PR (#27) that happened to touch non-markdown files triggered the first real CI run in this repo's history (issue #19) and immediately found 4 latent analyzer errors in the Phase 3/4 Grid Reserve code and 45 rules/map validation errors (dangling references to `E4`, removed in issue #14; wrong prerequisite ids on two Phase 5 buildings) — all fixed in the same pass. Every tagged release through alpha8 has shipped without a macOS build, and it took two fixes: `docs/BACKLOG.md` issue #29 found that the `macos` job in `.github/workflows/packaging.yml` was pinned to the `macos-13` runner image, which GitHub fully retired in December 2025, so the job queued forever and was silently auto-cancelled after 24h on every release — fixed by moving it to `macos-14`. That let alpha8's job actually acquire a runner and run for the first time, which surfaced a second, previously-invisible problem (issue #30): `packaging/macos/buildpackage.sh` compiled a "mono fallback" launcher variant (`apphost-mono.c`/`checkmono.c`) that doesn't exist in the pinned `ENGINE_VERSION` commit's `packaging/macos/` tree — that commit predates the engine adding mono-fallback support, and its `launcher.m` never references mono at all, so the fix was to drop the dead mono build/install steps entirely (matching what the pinned engine's own historical packaging script already did) and raise `MINIMUM_SYSTEM_VERSION`/the x86_64 apphost's clang target from 10.11 to 10.15 to match. Not yet verified against a real tagged release. Most recently, `docs/BACKLOG.md` issue #34 gave first-pass dedicated art to the rest of the Sungrid-original building/unit roster that still reused an unrelated existing actor's sprite (Cryptominer, Datacenter for AI, Drone Bay, Aerial Fabrication Bay, Grid Defense Turret, Smart Grid Relay, Resilience Shelter, Sensor Array, Wind Turbine Array, Hydrogen Plant, Recon Drone, Strike Drone) — generated programmatically via a new `mods/sungrid/bits/gen_concept_art.py`, the same approach issue #12 used for Solar Array, and deliberately fixing a real silhouette-collision bug along the way (`SGTUR`/`SGWND` had both been rendering the exact same sprite as the real `SAM` Site). `SGHAU` (Hauler Drone) was initially left on `HARV`'s chassis as an "easy adapt, not a new concept" call, matching the DISR/ARCT precedent from issue #14 — but that call was reversed in a follow-up pass once it became clear the identical sprite was a real gameplay-clarity bug (an unarmed Hauler Drone that's visually indistinguishable from an idle Ore Truck reads as "why is this not collecting ore?", since it collects Scrap, not Ore). `SGHAU` now has its own hex-chassis cargo-sled silhouette across all three `WithHarvesterSpriteBody.ImageByFullness` states (empty/half/full), generated the same programmatic way. A real local build/launch then caught two more issues invisible to CI: issue #35, a crash on shellmap load (`SGHAU`'s `harvest` sequence combined `Length` and `Facings`, which OpenRA multiplies into a 64-frame requirement, against a sheet that only ever had 8 — fixed by dropping the stray `Length`), and issue #36, `ARCT`/`DISR` still visually reading as Flame Tower/Flame Infantry despite issue #14's weapon/name swap, since that pass deliberately kept the original `ftur.shp`/`e4.shp` chassis art on a "the models were never the issue" call that turned out to be the same silhouette-identity mistake `SGHAU` had already been reversed for once. Both now have dedicated first-pass art too: `ARCT` a squat discharge base with a forked electrode (no nozzle/fuel-tank shape, including the build-up animation), `DISR` a fully self-contained 437-frame infantry sheet (abstract rotatable humanoid, backpack discharge cell, gold spark accent replacing the flame jet). A player report then found `GridReserveVault` was draining Credits into Reserve in every match regardless of the `gridreserve` lobby checkbox, since only `GridReserveController` (HUD/win-condition) actually read that toggle — fixed in issue #37 by having `GridReserveManager` resolve `Enabled` itself and gating the deposit tick on it. A same-day playtest with the checkbox genuinely on then surfaced a likely balance gap (issue #38, flagged for playtest, no numbers changed yet): the shipped `DepositRate` (~750 Credits/sec per Vault) looks far above normal economy income, which could mean a Vault absorbs ~all incoming Credits rather than just capping panic-dumps as `docs/GAME_MODES.md` describes. Grid Reserve is now on by default in the lobby (issue #39) — it's the mode that differentiates Sungrid Protocol's economy, so it's the standard experience rather than an opt-in extra, with destruction victory still fully intact and available regardless. A one-time, non-blocking in-match popup (`GridReserveBriefingLogic`, `Container@GRID_RESERVE_BRIEFING` in `ingame-player.yaml`) now explains the Core Rules to players when a match starts with the mode on, so the on-by-default flip doesn't drop players into it with no explanation. The entire programmatic sprite set (all 12 flat buildings including Solar Array, the turret, both drones, the Hauler Drone's three fullness variants, DISR's 437-frame sheet, and all 17 sidebar icons) then got a uniform quality pass (issue #40): 4x-supersampled rendering, consistent top-left key lighting, 1px readability outlines on all unit frames, cameo-card icons, and genuinely distinct damaged frames — the first pass had a bug (`two_frame_sheet` read each building's damage-blotch list before the draw function producing it had run) that made every building's damaged frame pixel-identical to its idle frame. Frame sizes/counts/layouts are unchanged, so no sequence YAML was touched; Solar Array's art is now also generated by `gen_concept_art.py`, closing the gap where issue #12's original generator script was never committed. Most recently (issue #43), the entire programmatic roster was switched from truecolor to **indexed** sprites on the stock RA player palette (`mods/sungrid/bits/temperat.pal`, committed for the generator): OpenRA renders indexed PngSheets through the actor's `player` palette, so the gold "grid-live" accent — mapped onto the player-color remap ramp (indices 80–95) — now recolors to each owner's team color like the ported stock buildings, instead of a fixed gold that ignored ownership (no rules/sequence changes needed). Build-menu cameos were also regenerated at full 64×48 (from 32×24). Indexed 1-bit alpha hardens the issue #40 anti-aliasing (reads as more consistent with stock RA sprites) and shifts the locked greens to their nearest RA-palette entries — accepted, since the "signature green" emphasis was dropped as outdated. A playtest then noted the Sungrid-original cameos lacked the actor-name label the ported stock RA cameos bake into their sprites (BARRACKS/SUB PEN/ORE REFINERY); issue #44 added it — `make_icon` now bakes an uppercase, word-wrapped name (in FreeSansBold, the build menu's own font) across the bottom of each of the 17 cameos over a darkened legibility strip, matching the stock style (cameos stay truecolor 64×48; no rules/sequence changes). Most recently (issue #45), the whole 17-cameo set was replaced with **photographic "real-style" cameos** cut from author-supplied concept renders (`docs/concept-art/cameo-sources/desert-base.png`/`desert_base2.png`/`grid-scene.png`): a new `mods/sungrid/bits/gen_photo_cameos.py` cover-fits a cropped subject per actor into the 64×48 frame and reuses `gen_concept_art.py`'s border + `draw_icon_label` so the baked name label is identical — closer to the rendered look of the ported stock RA cameos (cameos stay truecolor, only `*icon.png` changes, no rules/sequence edits; `gen_concept_art.py` kept as the programmatic fallback; source-art provenance tracked as first-pass placeholder in `docs/LICENSE_NOTES.md`). A player screenshot then flagged that those cameo labels still didn't match the stock ones in the same menu — multi-line and green-tinted vs. the stock single-line white; issue #46 harmonized them by rewriting `draw_icon_label` to bake a single **white** line (no word-wrap) sized from an 8px ceiling down to 5px to fit on one line, matching the ported cameos (BARRACKS/ORE REFINERY) beside them (label baker only, 17 `*icon.png` regenerated, no rules/sequence changes). A player screenshot then flagged that the Recycling Depot (RCYD) had been missed by the photo-cameo pass entirely — it renders its in-world sprite by reusing the stock `oilb.shp` derrick, so it had inherited `oilb`'s stock icon rather than getting a dedicated one; issue #47 added an 18th photo cameo (`rcydicon.png`, a scrap-metal crop, labeled "RECYCLING DPT") and gave RCYD its own `rcyd` sequence image mirroring `oilb`'s world sequences so the shared civilian `OILB` derrick keeps its stock icon and only RCYD's cameo differs. Most recently (issue #48), the player asked what it would take to bring the same quality bar to the low-level **in-world** sprites (not just cameos) and agreed to start with a 3-building test batch before committing to the rest of the roster: Solar Array, Advanced Solar Array, and Cryptominer were redrawn in `gen_concept_art.py` with genuine volume at the game's overhead camera angle — a new `tilted_collector` helper replaces the old flat front-elevation solar panels with a shaded, sky-gleaming parallelogram surface, and a new `capped_box` helper (lit top + shaded side + front face) replaces the flat storage cell/server racks, with Cryptominer's racks now drawn back-row-then-front-row so they overlap correctly. The team-color gold conduit band was deliberately kept on its existing flat grammar after an early cast-shadow attempt muddied the remap read. Frame sizes/counts are unchanged (sgpwr/sgcry 66×54×2, sgapwr 90×60×2), so no sequence YAML or cameo files were touched. The rest of the roster (and Recycling Depot's still-stock in-world sprite) is deferred to a follow-up pass. Most recently (issue #49), the remaining Phase 6 open items other than terrain/scenery (deliberately deferred) were closed out in one pass, with a live client actually built and run for the first time since issue #33: cursors (`mods/sungrid/bits/gen_cursor_art.py`) are now genuinely original "grid-glass" pointer art — truecolor PngSheets written directly into files literally named `mouse.shp`/`nopower.shp`/`attackmove.shp` (PNG content sniffing, not filename extension, so no palette work and no `.shp`-encoder round-trip needed), replacing issue #33's palette-tint-only pass; the main menu got its own mod-owned `chrome/mainmenu.yaml` override (superseding the fully-stock `common|chrome/mainmenu.yaml` layout issue #33 left in place) dropping the stock "Battlefield News" feed (pulls upstream OpenRA release announcements from `master.openra.net`, not anything about Sungrid) and the "Connect to an OpenRA forum account" prompt, replacing the news slot with a static tagline; and the main-menu music sting (`mods/sungrid/bits/gen_intro_music.py`) is now an original synthesized ambient loop replacing stock RA's `intro` track, via `music.yaml`'s `Extension: wav` plus a same-named override file in `sungrid|bits` (loaded after content packages specifically to allow this). The engine-build+`Launch.SkirmishBots` patch from issues #17/#33 was also pushed further than either prior attempt: a real 4-player bot match now runs to conclusion (confirmed via the post-game objectives panel showing distinct bot scores) with the human client correctly slotted and a real starting base actor placed at a real spawn location — strictly further than the "no bots, no starting units" wall issues #18/#33 hit. One new blocker surfaced in its place: even with all of that confirmed correct and the camera explicitly re-centered on the local player's spawn, the battlefield view still renders fully black — `World.RenderPlayer` resolves correctly but that player's `Shroud` reports nothing explored, root cause not found, scoped as its own follow-up (a third deferral of the same underlying "see a live temperate battlefield" goal) rather than a fourth open-ended debugging pass — see `docs/PLAYTESTING.md` for the full technical trail. A player screenshot then flagged a small gold-outlined box rendering next to the sidebar's cash readout, looking like a glitch; issue #50 traced it to `gen_chrome.py`'s money-bin background art drawing a purely decorative "recessed readout" rectangle directly underneath where the engine renders the real, live `Image@CASH_ICON` widget (`chrome/ingame-player.yaml`) at runtime — fixed by dropping the stray rectangle from both faction money-bin variants and regenerating `sidebar.png`; no rules/sequence/widget changes. The same screenshot also asked why both factions' radar-placeholder badge looked identical; issue #51 found `gen_chrome.py`'s `emblem()` already took a per-faction `accent` parameter but every actual draw call inside it was hardcoded to gold regardless, so Assembly's badge is now genuinely green (ring/hex/sun/rays recolored, horizon band kept as the shared green "grid" element) instead of a same-gold recolor in name only; a related "build-queue menu looks narrower than the panels above it" report was investigated and found to be real but not a bug — unmodified, stock-identical `ingame-player.yaml` geometry (the radar's 8px inset, the icon grid sharing its 238px width with the category-tab column) that stock's borderless background texture never made visible, but this mod's deliberately bordered "grid-glass" panels do — flagged as a possible border-styling follow-up rather than fixed outright. Asked to go further than a recolor, issue #52 gave each faction's radar-placeholder badge its own genuinely distinct mark grounded in `docs/BUILDINGS.md`/`docs/ART_DIRECTION.md`'s existing capital-technocratic/centralized (Consortium) vs. decentralized/improvisational/drone-based (Assembly) axis, rather than defaulting back to the shared sun/hex brand mark's shape: Consortium is now a "Citadel Seal" (fortress-wall hex, metering ticks, spokes converging inward to a diamond vault core), Assembly a "Swarm Rig" (three unevenly sized nodes in a peer mesh with a drone-rotor accent, no single hub) — no sun, no shared shape between them. The main-menu logo/loadscreen badge/mod icon keep the original neutral sun-over-horizon mark unchanged, since that's the shared game brand rather than a faction identifier. The alpha17 tagged release then closed out the last open release-packaging gap: `docs/BACKLOG.md` issues #21/#23 (Windows) and #29/#30 (macOS) had each fixed a real bug but were still waiting on a genuine end-to-end confirmation from an actual pushed release tag — alpha17's `Release Packaging` run is the first in this repo's history where all three platform jobs (Linux AppImages, Windows Installers, macOS Disk Image) completed with `conclusion: success` in the same run, confirmed via the GitHub Actions API. Issue #51's deferred style-direction follow-up was then actioned directly: `gen_chrome.py`'s hard-edged, full-contrast region borders across the sidebar (money-bin strips, icon-slot/support-power recesses, the radar-placeholder panels, and the production-icon row) were softened to low-contrast rounded frames (or, for the icon row, just a faint lit top edge with no side/bottom lines) — the same geometric width differences flagged in issue #51 are still there (unchanged, unmodified stock RA layout), but no longer read as a jarring step between hard-edged boxes of different sizes. `emblem_panel()` is shared with the loadscreen badge, so it picked up the same softer single-ring frame as a deliberate consistency call; `dialog.png`'s button-block borders (a different UI element) were left untouched. Two sub-faction identity renames followed directly on request: issue #54 renamed `DTRK`'s Ukraine sub-faction to Iran (a real-country swap, only `faction-ukraine`'s displayed name/description changed, every internal id/prerequisite/AI-weight/palette key left untouched — same minimal footprint the Allies→Consortium/Soviet→Assembly rename established), and issue #55 then renamed the four remaining real-world-coded sub-factions: England → The Ledger (counterintelligence), France → The Mirage (deception), and Germany → The Epoch (chronoshift) got fictional in-universe identities grounded in each one's existing special-unit fantasy, while Russia → China was a real-country swap on direct request, same pattern as Ukraine → Iran — plus the one unit-level nationality adjective in the tree, "British Spy," became "Auditor" to match The Ledger. Both renames are fluent-text-only; no rules/AI/palette/actor-id changes anywhere. Most recently, three passes landed together: the two faction logos were redone and rolled out to the lobby (issue #56) — `gen_chrome.py`'s issue-#52 marks (Citadel Seal/Swarm Rig) redrawn bolder with a size-aware detail tier, and a new `gen_flags()` patches them into the previously-stock `allies`/`soviet` slots of `glyphs*.png` (which until then still showed the Allied eagle and Soviet hammer-and-sickle in the faction picker); the sub-faction nation/flag mismatches were corrected on direct request (issue #57) — Greece replaces England (display name + the stock Greek flag art copied into the england slot), USA replaces France (display name + a programmatic US flag), and the already-renamed China/Iran factions got proper PRC/Iran flags instead of the USSR/UkSSR art they'd been flying, with all western nation flags otherwise kept and Germany ("The Epoch") deliberately untouched as the only remaining fictional sub-faction; and the volumetric in-world sprite pass got its batch 2 (issue #58): Wind Turbine Array (cylindrical masts, capped-box footings, volume blades with opaque motion-streak arcs — a translucent swept disc doesn't survive the indexed pipeline's 1-bit alpha), Grid Defense Turret (stepped-cylinder drum using only radially symmetric shading since all 32 facings rotate from one frame, with directional detail confined to the barrel), and Disruptor Trooper (cylindrical torso ramp, spherical helmet, capped backpack cell across the unchanged 437-frame layout) — frame metadata byte-compatible throughout, so no sequence YAML or cameo changes. Germany's sub-faction identity was then reverted from "The Epoch" back to plain Germany on direct request (issue #59) — fluent-text-only, no flag change needed since Germany's slot was never touched by the earlier flag corrections. A proposal to consolidate the European sub-factions (Greece, Germany, and any future additions) into a single EU faction, and Iran plus a fictional "Federation of the Middle East" (explicitly named as also containing Iraq and others) as the Assembly's counterpart, is now recorded in `docs/BACKLOG.md` issue #60 as a design question rather than implemented — it's a many-to-one consolidation that would shrink the lobby's selectable sub-faction list and force a decision about which existing special unit(s) each merged identity keeps, unlike every prior rename in this project, which was a same-scope 1:1 label/flag swap. A player then rejected the Disruptor Trooper's sprite outright ("looks too bad to be taken seriously"), asking for the other units' art to be studied and copied instead; issue #64 found that both earlier passes over this sheet (#36, #58) had reshaded a figure whose *construction* was wrong: its 8 facings were an image-plane **rotation** of one side-view drawing (fine for the top-down drones/turret the rest of `gen_concept_art.py` generates, but it rendered a man lying on his side or upside down in seven of eight directions), it stood ~23px tall against every stock infantryman's ~15 with its feet ~15px *below* the frame centre and its boots clipped off the frame, its 4x-supersampled LANCZOS edges were mush once the indexed pipeline hard-thresholded them, and it had no shadow. Decoding the stock art in-repo (`e6.shp` against `temperat.pal`, plus `heli.shp` to pin the frame-0-is-north/counter-clockwise facing convention) gave the actual recipe, and DISR is now rebuilt to it: a new native-resolution palette-**index** canvas (`PC`) draws the trooper one pixel at a time with no scaling or blending, at stock proportions (5x14, boots on the frame centre row), with all 8 facings drawn as genuine viewpoints, a baked `ShadowIndex`-4 blob at the feet, the body mass moved onto the 80-95 player-remap ramp the way stock infantry uniforms are (reversing issue #43's gold-only remap, which left almost nothing team-coloured on a 14px figure) with identity carried by fixed non-remap accents, grey sleeves/darker legs/near-black belt for stock's local-contrast read, and genuinely rebuilt animation (6-phase walk, 16-phase discharge with recoil, axis-aligned prone crawl, articulated die-collapse, dither dissolve instead of an alpha fade the 1-bit indexed alpha was silently discarding). Frame size and the 437-frame layout are byte-compatible, so no sequence YAML changed, and re-running the generator leaves every other sprite and cameo in `mods/sungrid/bits` byte-identical. The same request was then made of the two defence turrets, and issue #65 found the Grid Defense Turret had the *same* construction bug issue #64 had explicitly excused it from: decoding the stock rotating turret (`sam2.shp`) showed 227 pixels byte-identical across all 32 facings, because the mount is drawn once and only the superstructure is redrawn per facing as a genuine view of a solid with the key light fixed in world space — whereas `sgturturret.png` was one flat top-down drawing spun 32 times, so the drum, its shading and its vents rotated with the barrel, and (since the pivot is the frame centre and the drum sat 6px below it) the mount visibly orbited while tracking. Both turrets are now built on a new **axonometric mesh renderer** in `gen_concept_art.py` (`Mesh`: quads/boxes/prisms/struts, painter-sorted with back-face culling, flat per-face shading from a fixed world-space light, orthographic projection with the ground plane at a measured 2:1): `SGTUR` is a ring-mounted weapon station whose turntable is drawn with ellipses so it is pixel-identical in all 32 facings, with a hull, cap plate, team-coloured conduit band and a barrel mounted high and offset right of the pivot (so it stays visible over the roofline when pointing away, and the facing reads from the offset alone), and `ARCT` is a ring-mounted emitter head on a concrete pedestal with twin discharge rods and an arc bridging their team-coloured electrode heads. Both now bake a real `ShadowIndex`-4 ground shadow, which required composing the sheet through a new `indexed_strip()` — index 4 is a stencil and so is excluded from the RGB→index search, which is also why the rest of the roster's `contact_shadow()` calls (alpha 70, under the 1-bit indexed alpha threshold) have never produced a visible shadow. Damage lives in the silhouette (a shorter barrel with no muzzle collar; a snapped rod) rather than only in decals, and the discharge arc is drawn opaque since a translucent bloom is thresholded away by the same 1-bit alpha. Frame sizes/counts are unchanged (48×44×64, 40×36×2), so no sequence YAML was touched, both cameos stay the photographic ones, and every other sprite in `mods/sungrid/bits` is byte-identical after a full regen. A follow-up question — whether `ARCT` really had no turning sprites — was confirmed (it had a `Turreted:` trait that turned invisibly, since the discharge spawn point is rotated by it, but no `WithSpriteTurret`) and then acted on in issue #66: the emitter head is now split off into its own `arctturret.png` (40×36, 32 idle + 32 damaged facings, arc re-derived per facing from the projected rod tips), `arct.png` keeps only the fixed pedestal, and `ARCT`'s trait set now mirrors `SAM`/`SGTUR` exactly (`WithSpriteTurret:` added; `-QuantizeFacingsFromSequence:`/`BodyOrientation: QuantizedFacings: 8` swapped for `-BodyOrientation:`/`ClassicFacingBodyOrientation:`), with `turret:`/`damaged-turret:` sequences added. `TurnSpeed: 512` was deliberately left alone — the head snaps rather than sweeps, so the win is that a defence visibly points at what it is engaging. Most recently, the AI opponent was taught to play Grid Reserve at all (issue #67): nothing in the bot stack knew the mode existed, so a bot only ever built a Vault via `BaseBuilderQueueManager`'s stock silo-on-full-ore-storage path — a trigger unrelated to the Reserve target, topping out at one or two Vaults against a four-plus-Vault target, which made the economic victory human-only and quietly collapsed every bot skirmish back to destruction victory despite the mode being on by default since issue #39. A new `GridReserveBotModule` (per-`Player` `ConditionalTrait`/`IBotTick`) sizes the bot's Vault count off `GridReserveManager.Target` and `GridReserveVaultInfo.Capacity`, queues them one at a time into an idle production queue so `BaseBuilderQueueManager`'s own placement logic still does the placing, and reads every enemy's manager to bank harder when an opponent passes the 50% reveal threshold and to stop buying Vaults when one is holding Lockdown (the race is lost on the clock at that point, so the income is worth more as the army that can break it). It short-circuits on the same `Enabled` lobby gate issue #37 added, so bots are unchanged with the checkbox off. All four stock personalities get it at defaults (plus a `BuildingLimits: silo: 8` cap the overflow path never had), and a fifth lobby personality, **Grid Broker AI**, reuses every Normal AI module verbatim and differs only in over-building capacity and never conceding an opponent's Lockdown. Deliberately deferred: bots still don't preferentially raid enemy Vaults and the Turtle AI doesn't drop turtling against an imminent economic win — both need engine-level squad target-selection changes, so the module instead grants two optional conditions (`EnemyBankingCondition`/`EnemyLockdownCondition`) as the mod-side hook for trying that later. A playtest of that AI immediately found it was trivially easy — the bot built nothing but Vaults, no defenses and no army — which turned out to be issue #38's long-open `DepositRate` question finally confirmed with real data rather than an estimate (issue #68). `GridReserveVault` deposited with **no spendable floor at all** at ~750 Credits/sec per Vault, and every bot production path in the engine is gated on spendable cash at the same threshold (`BaseBuilderQueueManager`/`UnitBuilderBotModule` both bail under `ProductionMinCashRequirement` 500, and `NewProductionCashThreshold` 7000-8000 is never reached), so five-plus Vaults draining one treasury in parallel pinned the bot at zero cash permanently — it could not build a pillbox or a tank for the rest of the match. Aggravated by the Vault living in the **Defense** queue alongside `pbox`/`gun`/`tsla`/`sam`/`arct`, so a 150-Credit Vault trickling in at zero cash blocked every defense behind it. This was never bot-specific (a human with Vaults filling was equally locked out of spending); the AI just made it visible. Fixed in three parts: a new `GridReserveVaultInfo.MinimumOperatingBalance` (default 3000) so Vaults bank only the surplus above a spendable floor — holding across all of a player's Vaults, since they tick in sequence and each re-reads the balance; `DepositRate` 30 → 10; and bot pacing (`StartDelayTicks` 7500 so it builds a base and army before banking at all, `ExpandAtFillPercent` 80 so it adds the next Vault only once the standing ones are ~80% full, keeping one Vault filling at a time instead of five). The Grid Broker also gained its own `SquadManagerBotModule@gridbroker` (`SquadSize: 18` vs. Normal's 40) rather than reusing Normal's, since banking is income that could have been army and an unpressured opponent just out-banks it. **Still open on the next playtest:** the fix is verified only to the bar of "the bot is no longer paralysed" — that it builds defenses and an army again. It is *not* verified that a bot actually reaches Lockdown, which is a different and higher bar; the constants (floor 3000, rate 10, the 80% fill gate, the 5-minute start delay, and issue #67's `CoveragePercent`/`MaximumVaults`) are all reasoned from the engine's own cash gates rather than measured against observed income. If a bot now builds and fights but never threatens the Reserve target, that is a target/coverage tuning pass, not a repeat of this bug. That playtest then happened, and answered it: a bot **does** reach Lockdown now — but the screenshot showed it at 38,413 against a 30,000 target, which exposed a second bug (issue #69). Nothing stopped a Vault banking *past* the Reserve target: `GridReserveVault.Tick` filled to `Capacity` with no reference to `Target`, so issue #67's `CoveragePercent` (150 for the Grid Broker), which sizes Vault *capacity* above target as documented raid redundancy, became a *banked* buffer instead. That silently deleted the mode's core counterplay — `docs/GAME_MODES.md` Core Rule 4 says a Lockdown cancels when Reserve drops below target, typically from a Vault kill, which only holds while the total sits *at* target; at 38,413 across ~6 Vaults it took three kills inside the 90-second window to break. Fixed with a new `GridReserveVaultInfo.MaximumTargetPercent` (default 100) capping the owner's *total* Reserve, re-read every tick rather than latched so a raid re-opens banking and the owner must earn the Lockdown back. The player's other question — how the bot afforded a large army *and* all that banking — was investigated and is **not** a cheat (bots share the human's `PlayerResources`, no bonus/multiplier/handicap anywhere): issue #68's `MinimumOperatingBalance` floor makes a bot's economy *more* efficient than a human's, pinning its spendable cash near 3000 while converting every surplus Credit to Reserve, and since its build rate is queue-throughput-limited rather than cash-limited, that surplus was income it could not have spent anyway. Recorded as an interaction, not a defect — but it means the ceiling has a side effect (once at target the bot's cash climbs past `NewProductionCashThreshold` and it builds *more* production), so the Grid Broker's clock was slowed to compensate (`CoveragePercent` 150 → 125, `ContestedCoveragePercent` 175 → 150, `StartDelayTicks` 5000 → 9000 — it had been banking *earlier* than every other personality). The difficulty complaint itself is answered by a separate sixth lobby personality rather than by tuning the same one in two directions: **Easy Grid Broker AI**, which deliberately does *not* reuse the Normal AI's base modules, on the premise that a bot playing the economy well is still a full-strength Normal AI in the ground war and it is the ground war that beats a new player. It carries its own base builder (1 barracks/1 tent/1 war factory against 7/7/4, no superweapon or advanced tech), unit builder with hard `UnitLimits` on every line rather than the Normal AI's four, a flat non-random 25-unit attack force with a guaranteed rebuilding gap, half the opening harvesters, one base with no expansion, no support powers, and a Reserve economy that banks late and concedes an opponent's Lockdown. Worth noting for future AI work: `McvExpansionManagerBotModule` is what deploys the starting MCV, so a personality with no instance of it never builds anything at all. **Still unverified:** both the ceiling and the Easy Grid Broker's constants are reasoned from the engine's own config rather than measured, and the risk to watch on the easy bot is over-nerfing — one that never threatens the Reserve target at all is not a teaching opponent either. Most recently (issue #70), the Grid Reserve Vault itself finally got dedicated art: it is the `SILO` actor, so every prior art pass had skipped it and it still rendered stock RA's `silo2.shp` — a rusty open-topped bin filling with **ore**, for the building whose whole job is banking Credits as grid capacity. That is a different failure from the silhouette-collision bugs in issues #34/#36/#58/#64/#65 (two actors looking alike): this sprite was perfectly legible and simply described the wrong mechanic. Decoding `silo2.shp` first (24x24, 9 fill stages + 9 damaged, a baked `ShadowIndex`-4 blob, and a fill drawn in indices 83-91 — i.e. already inside the 80-95 player-remap ramp) pinned the layout to hit and confirmed team-colouring the level is the stock-consistent choice. The replacement `mods/sungrid/bits/sgvlt.png` (40x36, 18 frames, indexed) is a containerised battery energy storage system taken from the same hardware in `docs/concept-art/cameo-sources/desert_base2.png` — an olive switchgear cabinet with roof louvres behind a rank of three silver cell canisters — drawn on issue #40's building recipe. Three things are specific to it: charge is **double-coded** (a discrete 8-segment gauge, one segment per stage, plus a continuous bottom-up fill in each canister's sight window that is what actually reads at RTS zoom), and the *damaged* nine keep that readout dimmed rather than dark, both to avoid issue #40's identical-damaged-frames bug and because Core Rule 4 cancels a Lockdown when Reserve drops below target, so the level has to stay visible exactly while the Vault is being shot at; the gold accents are **re-stamped at native resolution** after the 4x LANCZOS downscale (`_vlt_accents`/`sgvlt_frame`), because the kernel reaches past a pixel's own block and the first pass produced a gauge whose segments alternated team-coloured and off-palette gold along its length; and the mass is drawn to the **24px tile** rather than the 40px frame, so the rows of 4-6 Vaults the AI builds (issue #67) tile into one continuous battery farm instead of colliding. No `SHADOW_IDX` shadow — the opaque shared ground pad means a real cast shadow would only show through the gaps *between* the canisters, reading as holes in the sprite — a painted contact shadow grounds it instead. The cameo is an 18th photographic one on issue #45's recipe (the battery bank cropped from `desert_base2.png`, issue #46's single-line white label). Wiring is minimal: `SILO` gains `RenderSprites: Image: sgvlt` and a new `sgvlt:` sequence node mirrors the stock frame layout (`stages` 0-8, `damaged-stages` 9-17, `make` frame 0, the stock `mbSILO` bib), with the stock `silo:` node left intact rather than repointed (the same split issue #47 used for `rcyd` vs. the shared civilian `oilb` derrick). Verified with a real engine+mod build in this environment: `./utility.sh --check-yaml` exits 0 across all maps, `--check-missing-sprites` reports no missing mod-owned sprite, and `--png-sheet-export` confirms the engine's own PNG reader parses `FrameSize: 40,36`/`FrameAmount: 18`; re-running both generators leaves every other sprite and cameo byte-identical. **Not verified in a live client** — this environment has no RA game content installed, so the sprite has not been seen in an actual match. Asked whether any other actors needed the same treatment, issue #71 ran issue #70's "does this sprite describe what the building actually *does*?" check across the roster and found two more failures of that kind (legible art, wrong mechanic — not the silhouette collisions of #34/#36/#58/#64/#65). **Recycling Depot (`RCYD`)** still rendered stock `oilb.shp`, an oil pumpjack, for a Scrap refinery that docks `SGHAU` and stores what it brings in — issue #47 had given it a dedicated cameo but deliberately left the world sprite on the derrick, so after #70 it was the last Sungrid-original-role building on borrowed art, and a 1×1 building wearing 2×2 civilian `OILB` art at that. It now has `mods/sungrid/bits/rcyd.png` (40×36 × 18, indexed) on the Vault's stage layout: a sorting hall under a wide-mouthed shredder hopper over an open tipping bay, with fill double-coded as a countable 8-segment gauge plus a centre-peaked scrap heap that grows with the level, damaged stages kept dimmed rather than dark, and the hopper's near lip shearing off when damaged so the state reads from the outline. **Smart Grid Relay (`SGREL`)** failed the check in the subtler direction — its relay pylon with radiating distribution lines depicted the cluster-pooling mechanic `docs/BUILDINGS.md` records as *explicitly descoped*, while the shipped trait set is a flat `+60 Power` and nothing else; redrawn as a pad-mounted transformer. That redraw exposed a second bug on the same actor: its gold conduit band was not team-coloured at all, because `draw_gold_band` only survives the 4× LANCZOS downscale on the 2×3 buildings' 8px band — on a 1×1 plinth the band is 3px, every row is an edge row, and the whole thing landed on fixed palette entries (fixed with #70's native re-stamp via a new `ACCENT_FRAMES` hook; remapped pixels 57 → 181, confirmed by simulating `PlayerColorPalette`). Wiring is minimal and mirrors #70: the `rcyd:` sequence node moves onto `rcyd.png` with `stages`/`damaged-stages`/`make`, the stock `oilb:` node is left intact for the shared civilian derrick, `RCYD` swaps `WithSpriteBody` for `WithResourceLevelSpriteBody`, and its bib becomes the `mbSILO` minibib every other 1×1 building here uses (it had been drawing a 3-cell-wide `bib3` under one cell). Verified with a real engine+mod build in this environment — `--check-yaml` exits 0 across all maps, `--resolved-rules`/`--resolved-sequences` confirm the trait and sequence swaps, `--png-sheet-export` confirms the engine's own PNG reader parses both sheets, `--check-missing-sprites` reports no missing mod-owned sprite and the total *drops* 3161 → 3157 now that the depot no longer references `oilb.shp`, and re-running both generators leaves every other sprite and cameo byte-identical. **Not verified in a live client** (no RA content installed here). That pass also found a genuine roster-wide bug, filed as issue #72 rather than fixed: `contact_shadow()` doesn't merely fail to draw a shadow (which #65 knew) — at alpha 70 it falls under the 1-bit indexed alpha threshold and **erases** the ground pad beneath it, leaving a transparent lens through the sprite; reproduced on untouched committed art (`sgsns.png`). Fixing it changes every affected sprite, so it belonged with issue #48's batch-3 volumetric pass — which is where it was then done. Most recently (issue #73), that batch 3 landed and closed issue #48's deferred list end to end: the last six Sungrid-original buildings (Datacenter for AI, Drone Bay, Aerial Fabrication Bay, Resilience Shelter, Sensor Array, Hydrogen Plant) and both flying drones were redrawn volumetrically, adding two shapes to the `capped_box`/`tilted_collector`/`Mesh` vocabulary — `Roof` (a receding top plane with an `at(u, v)` mapping so rooftop plant sits on the roof in perspective rather than floating over the eaves) and `sphere()` (nested ellipses converging on a small off-centre highlight, replacing `dome3d`'s three hand-placed blobs, with a deliberately gamma-curved lit half since a linear ramp reads as gloss rather than curvature). The silhouette calls that matter: `SGDAI` and `SGDRA` are both enclosed halls on the same footprint, so one got a flat plant-covered roof and the other a barrel vault; `SGDRN`'s 1px A-frame trusses (effectively invisible on terrain) became box-section masts and beam over an apron slab whose *top* plane the parked drone stands on, with its rotors drawn **stopped** since a parked airframe isn't spinning and two clean blades read where a swept ring turns to noise at 16px; `SGSNS`'s dish became a genuine concave bowl lit on its lower-right inner face (what a dish tilted up-and-left does under this key light, and the cue that tells a bowl from a disc at 20px). Issue #72 was fixed in the same pass: `contact_shadow()` now paints opaque — `dim(base, f)` of whatever it lands on, with `base=SUN_GOLD` for pools on the conduit band so the shadow stays on the remap ramp instead of putting a grey bar across a team-coloured strip. Measured: transparent pixels inside the conduit band went 28–161 per sheet → **0** on all eight buildings that have one, and inside the concrete pad 132 (`sgshl`)/100 (`sgsns`) → **0** on the two that don't. Its second clause — "the buildings that should cast a shadow bake a real `SHADOW_IDX` one" — was answered rather than implemented, with evidence: `indexed_strip` only writes the stencil where the body frame is transparent, so a shadow projected onto the building's own pad is discarded outright, and decoding stock `fact.shp` against `temperat.pal` shows RA buildings bake no long cast shadow at all, just a 1–3px `ShadowIndex` rim along the lower edge of a silhouette that already fills its frame. The turrets keep their real `SHADOW_IDX` shadows (issue #65) because a pedestal genuinely leaves terrain visible around it. The same 1-bit-alpha rule finished the drones, whose translucent rotor discs were being deleted outright (leaving a body ringed by four empty circles) and are now opaque blades with one trailing tip streak. Frame sizes/counts are unchanged throughout (66×54×2, 72×50×2, 40×36×2, 90×60×2, 32×30×32, 36×32×32), so no sequence YAML or cameo files were touched, and `sgpwr`/`sgapwr`/`sgcry`/`sgwnd` regenerate only as collateral of the shadow fix. Verified here with `--check-yaml` (exit 0 across all maps), `--png-sheet-export` (the engine's own PNG reader parses every sheet at its unchanged frame layout) and `--check-missing-sprites` (no missing mod-owned sprite); re-running both generators leaves every other sprite and cameo byte-identical. **Not verified in a live client** — no RA content installed here. Most recently (issue #74), the work moved from *pictures* to **sprite states** — the set of things the engine already asks each actor to draw, which every prior pass had left answered by stock art or by a single held frame. The audit that opened it found three: no Sungrid building had a build-up (11 of 13 held `Start: 0, Length: 1`, so they popped in fully formed, while `SGTUR` and `SGDRA` borrowed a *different* building's stock `gunmake.shp`, against 9–13 frames on every ported stock building beside them); the three buildings with death rubble pointed at stock `powrdead.shp`/`apwrdead.shp`; and, worst, the husks silently undid an earlier fix — `SGHAU` died into `HARV.EmptyHusk`/`HARV.FullHusk`, i.e. straight back into the **Ore Truck wreck** that issue #34's follow-up had given the Hauler Drone its own chassis to stop colliding with, while `SGDRO` left a Chinook and `SGDRS` a Black Hawk. Conventions were pinned by decoding the stock art in-repo first (`fcommake.shp`, `gapmake.shp`, `powrdead.shp`, `apwrdead.shp`, `hhusk.shp`, `hhusk2.shp` against `temperat.pal`), the same way issues #64/#65/#70 established theirs. Fixed with 21 new sheets, all from `gen_concept_art.py`: **15 build-ups** (`*make.png`, 9 frames), where `make_frames()` is deliberately a bottom-anchored vertical scale of the finished sprite plus a monochrome `construction_tint` rather than a hand-drawn scaffold sequence — decoding `fcommake.shp` shows RA's own build-up *is* a squat version of the whole building growing to full height at constant footprint width, so deriving it keeps 15 build-ups in step with their draw functions instead of needing 15 more of them, and since greys sit far from the gold reference ramp nothing in a half-built structure is team-coloured until the finished frame lands; **3 rubble sheets** (`sgpwrdead`/`sgapwrdead`/`sghyddead`, 1 frame each, no concrete pad because `WithBuildingBib` keeps drawing the bib underneath — which is why stock rubble has none either) each keeping two or three recognisable pieces of the building plus a severed gold conduit, since stock rubble carries remap-ramp pixels too; **2 husk sheets** (`sghauhusk`/`sghauhuskfull`, 32 facings at the sled's own 34×28) plus four new husk actors, where the two drones' husks render *their own sprite* because that is the stock helicopter-husk convention (`TRAN.Husk`/`MH60.Husk` do the same — the wreck is the spin-down and the ground explosion, not a separate burnt sheet), which is also why their fluent names stay "Recon Drone"/"Strike Drone"; and **`sgturpad.png`**, since `SGTUR`'s body sequence was stock `gunmake.shp` for both `idle:` and `make:`, so giving it a build-up meant giving it a pad — `WithSpriteTurret` is already gated on `!build-incomplete`, so the build-up shows the pad alone and the station pops in on completion, exactly how `SAM`/`GUN`/`AGUN` behave. A new `silhouette_shadow()` derives the `SHADOW_IDX` stencil from a frame's own silhouette offset down-right; issue #73's "buildings get a painted pool, not a stencil" rule generalises to *whether the sprite fills its own frame*, and a wreck or a husk is the case where it doesn't (stock agrees: `powrdead.shp` bakes 77 `ShadowIndex` pixels, `hhusk2.shp` one per facing, `fact.shp` none). Verified with a real engine+mod build here — `--check-yaml` exits 0 across all maps, `--png-sheet-export` parses all 21 new sheets at the intended layout, `--resolved-rules`/`--resolved-sequences` confirm the four new husk actors and every rewired `make:`/`dead:`/`idle:` node, `--check-missing-sprites` reports no missing mod-owned sprite and the total *drops* 3157 → 3149; re-running both generators leaves every other sprite and cameo byte-identical. **Not verified in a live client** — no RA content installed here. Still open and deliberately untouched, and scoped as batch 5: the **animated** half of the same audit — there is no `WithIdleOverlay` anywhere on a Sungrid actor, against 27 animated `idle:`/`active:` nodes among the ported stock buildings, so `SGWND`'s blades don't turn and `SGSNS`'s dish doesn't sweep; the `grid-normal`/`grid-strained` power-tier states on `SGCRY`/`SGDAI`/`SGTUR` and `drone-uplink`/`drone-uplink-degraded` on both drones change output, `DetectCloaked` and armament with no visual cue; `ARCT` has no `WithMuzzleOverlay` at all so the Arc Turret fires with no flash; both drone bodies bake four rotors *and* mount stock RA rotor overlays on top, so a drone in flight wears two sets of blades; and `SGHAU`'s fullness sheets still use the abstract hex sled rather than the splash's flatbed-with-scrap-load. See `docs/BACKLOG.md` for the full engineering issue list (GitHub Issues is disabled on this repo, so they're tracked there instead of as real issues until it's enabled).

## Working conventions

- Never edit or commit anything under `engine/` — it's fetched by `fetch-engine.sh` and gitignored. If a friction point genuinely needs an engine-level change, see "Engine version pinning" below (usually an `engine-patch/*` branch, not a personal fork).
- Prefer YAML/Lua trait composition in `mods/sungrid` over new C# traits in `OpenRA.Mods.Sungrid`; if a new trait seems necessary, check it against the "New C# traits" row in `docs/ARCHITECTURE.md` first.
- Keep commits/PRs scoped to one phase/issue at a time — see `docs/CONTRIBUTING.md` for the full PR checklist and label taxonomy.
- `main` is now the default/integration branch (previously `bleed`, inherited from the original OpenRA engine fork; `bleed` has since been deleted from the remote — its history lives on as `main`'s own ancestry, since `main` was built on top of it rather than starting fresh). Never push directly to `main`; always branch + PR.

## Engine version pinning: how `ENGINE_VERSION` actually works (read before touching `engine-patch/*` branches)

`mod.config`'s `ENGINE_VERSION` + `AUTOMATIC_ENGINE_SOURCE` do **not** point at a separate personal engine fork repo. They point at a specific commit SHA inside `richardkfm/sungrid-protocol`'s **own git history** — an ancestor of `main` predating the Phase 0 SDK migration (`e96d2ae`), from back when this repo still had the full vendored engine tree. `fetch-engine.sh` downloads that exact commit as a GitHub archive-by-SHA zip (`https://github.com/richardkfm/sungrid-protocol/archive/${ENGINE_VERSION}.zip`). See `docs/BACKLOG.md` issue #20 for the full history of why this exists (a `CS0121`/`CryptoUtil.SHA1Hash` overload ambiguity under some .NET 8 SDK patch versions, fixed with a one-line disambiguation plus a follow-up `IDE0301` suppression).

**`engine-patch/*` branches (e.g. `engine-patch/bf4102a-cryptoutil-fix`) exist only to keep one specific pinned engine commit reachable for that archive-by-SHA fetch. They are not mod-content branches.**

- **Never merge one into `main`.** Doing so would try to re-add the entire pre-SDK-migration vendored engine tree (`OpenRA.Game/`, `OpenRA.Mods.Common/`, etc.) that Phase 0 deliberately removed. GitHub will correctly report this as a conflicting/`dirty` merge — **that's expected, not a bug to fix or a conflict to resolve.**
- **Never delete one.** Deleting the branch risks GitHub garbage-collecting the commit it points at (no longer reachable from any ref), silently breaking `fetch-engine.sh`/CI for every future build pinned to that SHA.
- **This exact mistake has already happened twice**: once during issue #20's original implementation, and again as PR #33 ("Engine patch/bf4102a cryptoutil fix"), opened a second time against `main` and correctly showing `mergeable_state: dirty`. Investigation confirmed the fix it carried was already the pinned `ENGINE_VERSION` commit and already verified working — the PR was redundant, not a real conflict to resolve. **If you see a PR like this again: close it without merging, comment pointing at this section, and do not delete its source branch.**

**To ship a *new* engine-level fix in the future:** branch from the currently-pinned `ENGINE_VERSION` commit, add the fix, and push it as a new `engine-patch/<description>` branch — never opened as a PR against `main`. Then open a normal mod-content PR that only updates `mod.config`'s `ENGINE_VERSION`/`AUTOMATIC_ENGINE_SOURCE` to the new SHA; that `mod.config` change is the only part of this process that goes through normal review and merges into `main`.
